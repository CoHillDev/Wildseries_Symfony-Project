{# {% extends 'base.html.twig' %}

{% block title %}All categories{% endblock %}

{% block body %}
    <h1>All the wild categories : </h1>
    {% for category in categories %}
        <div>
      <p><a href="{{ path('category_show', { 'categoryName': category.name }) }}"> ☞ {{ category.name }}</a></p>
        </div>
    {% else %}
        No categories found
    {% endfor %}

    <a href="{{ path('app_index') }}">🏠 Back to Home</a>
{% endblock %} #}

{% extends 'base.html.twig' %}

{% block title %}All categories{% endblock %}

{% block body %}
    <h1>All the wild categories : </h1>
    
    <div class="row">
        <div class="col-md-6">
            <ul class="list-group">
                {% for category in categories %}
                    <li class="list-group-item bg-black text-white">
                        <p><a href="{{ path('category_show', { 'categoryName': category.name }) }}">☞ {{ category.name }}</a></p>
                    </li>
                {% else %}
                    No categories found
                {% endfor %}
            </ul>
        </div>
    </div>

    <a href="{{ path('app_index') }}">🏠 Back to Home</a>
{% endblock %}